%h1 Survey

%table.table.table-striped
  %tbody
    - if current_user
      - @questions.each do |q|
        %tr
          %td= q.question
        %tr
          %td= rating_for q, "answer", :enable_half => true, :half_show => true, :disable_after_rate => false
  
        
%button.btn.btn-success
  Send